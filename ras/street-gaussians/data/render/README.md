This directory contains config files for rendering trained scenes, with
edits applied to trajectories.

Our first attempt at specifying simple edits is called "v1". It offers a
few simple ways in which trajectories can be modified. In the future, we'll
most likely want to do something much more sophisticated.
Modifications for different scenes using this system can be found in
`edit_v1/`.
